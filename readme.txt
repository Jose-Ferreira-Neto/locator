DEPENDECIAS ESPECIFICADAS NO package.json
template engine: handlebars, importando com o próprio e express-handlebars
setando como template engine
app.use('view engine', 'handlebars')
//define o layout da engine
app.engine()